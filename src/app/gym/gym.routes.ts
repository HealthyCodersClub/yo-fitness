import { NgModule } from '@angular/core';
import { Routes } from '@angular/router';

import { GymComponent } from './gym.component';

export const routes: Routes = [
    { path: 'path', component: GymComponent },
];